<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ProjectManagerMobile.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ProjectManagerMobile.Controls"
    xmlns:ic="clr-namespace:FluentIcons.Maui;assembly=FluentIcons.Maui">

    <ContentPage.ToolbarItems>
        <ToolbarItem>
            <ToolbarItem.IconImageSource>
                <ic:FluentImageSource Icon="Navigation" IconVariant="Filled" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Image
                Aspect="AspectFit"
                HeightRequest="185"
                SemanticProperties.Description="dot net bot in a hovercraft number nine"
                Source="dotnet_bot.png" />

            <Label
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource Headline}"
                Text="Hello, World!" />

            <Label
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                SemanticProperties.HeadingLevel="Level2"
                Style="{StaticResource SubHeadline}"
                Text="Welcome to &#10;.NET Multi-platform App UI" />

            <Button
                x:Name="CounterBtn"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill"
                SemanticProperties.Hint="Counts the number of times you click"
                Text="Click me" />

            <Entry />

            <controls:OutlinedEntryControl Placeholder="My text" />
            <controls:OutlinedEntryControl Placeholder="My text" />
            <controls:OutlinedEntryControl Placeholder="My text" />
            <controls:OutlinedEntryControl Placeholder="My text" />

            <BoxView BackgroundColor="Black" HeightRequest="10" />

            <controls:FilledEntryControl
                FontSize="18"
                Placeholder="My text"
                PlaceholderFontSize="12"
                PlaceholderBackgroundColor="LightGray"
                PlaceholderTranslationY="-14" />

            <Border
                x:Name="MainBorder"
                Grid.Row="0"
                Padding="16,6"
                BackgroundColor="Gray"
                Stroke="Transparent"
                StrokeShape="RoundRectangle 24">
                <Entry />
            </Border>

            <BoxView
                Margin="0,50"
                BackgroundColor="Black"
                HeightRequest="10" />




        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
